import React from "react";
import { VendorOnboardingStepProps } from "../types";

const ContactStep = ({ form }: VendorOnboardingStepProps) => {
  return <div>ContactStep</div>;
};

export default ContactStep;
