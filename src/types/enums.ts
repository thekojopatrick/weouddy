export enum MediaType {
  IMAGE = 'IMAGE',
  VIDEO = 'VIDEO',
}

export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
  PARTNER = 'PARTNER',
  MEMBER = 'MEMBER',
}

export enum Role {
  ADMIN = 'ADMIN',
  MODERATOR = 'MODERATOR',
  MEMBER = 'MEMBER',
}

export enum EventActivityType {
  JOIN = 'JOIN',
  LEAVE = 'LEAVE',
  POST = 'POST',
  LIKE = 'LIKE',
  COMMENT = 'COMMENT',
  DELETE = 'DELETE',
  REPORT = 'REPORT',
}

export enum SharePlatform {
  Twitter = 'twitter',
  Facebook = 'facebook',
  Email = 'email',
  WhatsApp = 'whatsapp',
}
