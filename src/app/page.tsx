import EmptyPaused from "@/components/empty/empty-paused-state";
import React from "react";

const page = () => {
  return (
    <main>
      <EmptyPaused />
    </main>
  );
};

export default page;
